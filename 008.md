
-- 1. Añadir campos requeridos a la tabla sucursales
ALTER TABLE sucursales 
ADD COLUMN IF NOT EXISTS rif TEXT,
ADD COLUMN IF NOT EXISTS es_principal BOOLEAN DEFAULT FALSE;

-- 2. Asegurar que solo una sede pueda ser marcada como principal (Opcional pero recomendado)
-- CREATE UNIQUE INDEX IF NOT EXISTS idx_sucursal_principal_unica ON sucursales (es_principal) WHERE (es_principal = TRUE);

-- 3. Comentario legal
COMMENT ON COLUMN sucursales.rif IS 'Registro de Información Fiscal de la sede o sucursal';
